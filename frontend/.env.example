# Copy to .env (or .env.production) and set for a separate-origin deploy.
# Absolute backend API origin, no trailing slash, e.g. https://api.example.com
# Leave unset in dev — the Vite proxy serves the API same-origin.
VITE_API_BASE_URL=
