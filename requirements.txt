pandas
python-dotenv
sentence-transformers
faiss-cpu
torch
groq
openai
google-generativeai
tqdm
nltk

langchain-text-splitters
numpy
transformers
accelerate
bitsandbytes
pypdf
python-docx
beautifulsoup4
lxml
pdfminer
pdfminer.six

streamlit
fastapi==0.110.0
uvicorn==0.29.0
sqlalchemy==2.0.29
alembic==1.18.4
psycopg2-binary
slowapi==0.1.9
redis==5.0.8  # shared rate-limit storage for multi-instance deploys (slowapi)
resend==2.30.1  # transactional email (password-reset)
sentry-sdk==2.20.0  # error monitoring (optional at runtime — see backend/monitoring.py)
pgvector==0.4.2  # vector(768) column type for RAG embeddings
# Pinned: the test suite drives routes over httpx.ASGITransport, whose API
# changed at 0.28 (see CLAUDE.md). A clean CI install resolving a newer httpx
# breaks the test client; 0.28.1 is the version the suite is green on.
httpx==0.28.1  # web-page fetch for URL KB ingestion + the test ASGI transport

#  AUTH
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
cryptography==46.0.5  # Fernet — encrypts mailbox credentials at rest
email-validator==2.3.0

python-multipart==0.0.22

tf-keras
